# Frontend không cần secret. Cloudflare Pages Functions dùng biến/binding cấu hình
# trong dashboard hoặc Wrangler; xem .dev.vars.example cho local development.
# Không commit .env, .dev.vars, API token hoặc Access service token.
